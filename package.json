{
  "name": "hive",
  "version": "1.0.0",
  "private": true,
  "description": "Family Kanban board powered by Google Sheets",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "test": "cd apps-script && npm test && cd ../frontend && npm test",
    "test:apps-script": "cd apps-script && npm test",
    "test:frontend": "cd frontend && npm test",
    "deploy:apps-script": "cd apps-script && clasp push && clasp deploy"
  }
}
